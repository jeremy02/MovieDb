<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.MovieDb.app">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="false"
        android:icon="@drawable/ic_launcher_96"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher_96"
        android:supportsRtl="true"
        android:theme="@style/Theme.MovieDb">

        <activity android:name="com.MovieDb.app.search.SearchActivity" />

        <activity
            android:name="com.MovieDb.app.splash.SplashActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="com.MovieDb.app.main.MainActivity" />

        <activity android:name="com.MovieDb.app.detail.DetailMediaActivity" />
    </application>

</manifest>